module.exports = {
  soma(a, b) {
    return console.log(`${a} + ${b} é igual a ${a + b}`);
  },
  multiplica(a, b) {
    return console.log(`${a} * ${b} é igual a ${a * b}`);
  },
  subtrai(a, b) {
    return console.log(`${a} - ${b} é igual a ${a - b}`);
  },
  divide(a, b) {
    return console.log(`${a} / ${b} é igual a ${a / b}`);
  },
};
