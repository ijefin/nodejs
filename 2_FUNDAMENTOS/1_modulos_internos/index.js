const math = require("./modules");

const somar = math.soma;
const divisao = math.divide;
const subtracao = math.subtrai;
const multiplica = math.multiplica;

somar(50, 15);
divisao(40, 2);
subtracao(12, 2);
multiplica(15, 4);
